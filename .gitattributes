# 改行の扱い。
#
# **.sh は必ず LF で配ること。** CRLF で配られると、Mac / Linux では
# 1 行目が「#!/bin/sh\r」になり、**「そんなものは無い」と言われて動かない。**
# 見た目は何ともないので、これも黙って起こるたぐい。
#
# .bat は逆に CRLF。古い cmd は LF だけの .bat を読み違えることがある。
*.sh   text eol=lf
*.bat  text eol=crlf
